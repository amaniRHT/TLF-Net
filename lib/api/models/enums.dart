enum Answers { yes, no }
enum Gender { male, female }
enum FundingTypes { car, oldCar, newCar, materialOrEquipement, landOrLocals }
enum CarStates { notSpecified, oldCar, newCar }
enum Identity {cin, residencePermit, }
enum Operation { creation, edition, previewing, complementing }
